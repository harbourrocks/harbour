export const environment = {
  production: true,
  oidcDiscoverUrl: '${OIDC_DISCOVERY_URL}',
  oidcClientId: '${OIDC_CLIENT_ID}',
  oidcRedirectUrl: '${UI_ROOT}/auth/oidc/callback',
  graphQlUrl: '${GRAPH_QL_URL}'
};
